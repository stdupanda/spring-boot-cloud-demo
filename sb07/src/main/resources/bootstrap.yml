logging:
  file: ./sb07.log
  level:
    root: info
    cn: debug
server:
  port: 8087
spring:
  cloud:
    config:
      uri: http://localhost:8085/config
      name: db
      profile: dev
      fail-fast: true